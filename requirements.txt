# ── CUDA wheels index ─────────────────────────────────────────
--extra-index-url https://download.pytorch.org/whl/cu124

# ── core ───────────────────────────────────────────────────────
torch==2.5.1
torchvision==0.20.1
torchaudio==2.5.1

transformers==4.53.0
peft==0.10.0
trl==0.8.6
accelerate==1.9.0
bitsandbytes==0.47.0 ; platform_machine == "x86_64"
pyyaml                         # for `import yaml`

# ── math-verify + parser runtime ───────────────────────────────
math-verify[antlr4_9_3]==0.8.0
antlr4-python3-runtime==4.9.3

# ── CLI / config helpers ───────────────────────────────────────
tyro>=0.9.28
omegaconf==2.2.3

# ── Hugging Face dataset stack ────────────────────────────────
datasets>=2.19.0
evaluate>=0.4.2
huggingface_hub>=0.23.0
sentencepiece>=0.2.0
pyarrow>=15.0.2
tqdm>=4.66.4
pandas>=2.2.2
numpy>=1.26.4,<2.0  # Keep NumPy 1.x for TensorBoard/SciPy compatibility

# ── optional vision extras ──────────────────────────────────────
timm==0.9.16
einops>=0.7.0

tensorboard>=2.20.0
colorama>=0.4.6